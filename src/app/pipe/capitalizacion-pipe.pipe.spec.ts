import { CapitalizacionPipePipe } from './capitalizacion-pipe.pipe';

describe('CapitalizacionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizacionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
